
##                                     _                 _
##                                __ _| |__   ___   ___ | | ___ __ ___
##                               / _` | '_ \ / _ \ / _ \| |/ / '__/ __|
##                              | (_| | |_) | (_) | (_) |   <| | | (__
##                               \__,_|_.__/ \___/ \___/|_|\_\_|  \___|
##  ===============================================================================================
#   TITLE : abookrc
#   ABOUT : configuration file for abook
#   LINKS : -
##  ===============================================================================================

    field Telefoon = Telefoon, string
    field Enail = Enail, string

##  Note: if you don't define any view, abook will use a default display based on the above standard fields.
    view CONTACT = name, Telefoon, Enail

##  index_format=format_string
##  Defines the way entries are displayed in the main list. This is a string containing field names enclosed between braces, with  an  optional  width  limit
##  specified by a number (right alignment if negative) after the field name and a colon, and an arbitrary number of alternative fields (first with non empty
##  content is to be displayed) separated by vertical bars. For instance:

    set index_format=" {name|20}                           {Telefoon|20}                               {Enail|40}"

##  www_command=command
##  Defines the command used to start the web browser to view a contact's URL. Default is "lynx".

    set use_mouse= true

##  scroll_speed=lines
##  Defines the number of lines the address list is scrolled by on a mouse wheel action. This option only takes effect if use_mouse is enabled. Default is 2.

    set use_colors= true

##  Color settings:
##  Where color can be: default, black, red, green, yellow, blue, magenta, cyan, white
    set color_header_fg=white
    set color_header_bg=black

    set color_footer_fg=red
    set color_footer_bg=black

    # De lijst met contacten
    set color_list_even_fg=white
    set color_list_even_bg=black
    set color_list_odd_fg=white
    set color_list_odd_bg=black

    # De koppen boven de kolommen
    set color_list_header_fg=red
    set color_list_header_bg=black

    # De geselecteerde regel (ook wit, maar met een grijze achtergrond zodat je het ziet)
    set color_list_highlight_fg=yellow
    set color_list_highlight_bg=red

    # Het detailscherm (als je een contact opent)
    set color_tab_border_fg=red
    set color_tab_border_bg=black
    set color_tab_label_fg=yellow
    set color_tab_label_bg=black

    set color_field_name_fg=white
    set color_field_name_bg=black
    set color_field_value_fg=white
    set color_field_value_bg=black

